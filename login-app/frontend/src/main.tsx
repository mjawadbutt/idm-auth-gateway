import React from 'react'
import ReactDOM from 'react-dom/client'

// TODO: implement App component
const App: React.FC = () => <div>Login App</div>

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
